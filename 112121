local Tab = Window:Tab({
    Title = '功能',
    Icon = 'bird',
    Locked = false,
})

local Slider = Tab:Slider({
    Title = "Slider",
    Step = 1,
    
    Value = {
        Min = 15,
        Max = 120,
        Default = 70,
    },
    Callback = function(value)
        print(value)
    end
}) 
